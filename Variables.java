public class Variables {

    // Erstellung des Einstiegspunkts
    public static void main(String[] args) {
        // Variable deklarieren
        String firstName;
        int x, y, z;

        // Initialisieren
        firstName = "Adolf";
        x = 1;
        y = 2;
        z = 3;

                // Deklaration + init(ialisieren)
        char a = 'a';
        int p = 2, q = 3;

        // variablen Wert tauschen
        firstName = "Max";

        // anderer Datentyp -> Fehler
        //firstName = 1


    }
}
